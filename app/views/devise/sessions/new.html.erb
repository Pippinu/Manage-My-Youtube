<!DOCTYPE html>
<html amp >
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1">
  
  <title>ManageMyYoutube</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" rel="stylesheet">

</head>
<body>
<section class="vh-100" style="background-color: #ff5656 ;">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card shadow-2-strong" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">

            <h3 class="mb-5" style="text-decoration:underline">Login</h3>
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                <div class="field">

                    <%= f.email_field :email, autofocus: true, autocomplete: "email",placeholder: "Email",class:"form-control form-control-lg" %>
                </div>
              

                <div class="field">
                    <%= f.password_field :password, autocomplete: "current-password",placeholder: "Password",class:"form-control form-control-lg"  %>
                </div>
                
                <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                  <div class="form-check d-flex justify-content-start mb-4 mt-3">
                    <%= link_to "Hai dimenticato la password ?", new_password_path(resource_name), class:"link-secondary",style:"margin:0" %><br />
                  </div>
                <% end %>

                <% if devise_mapping.rememberable? %>
                    <div class="form-check d-flex justify-content-center mb-4 mt-3">
                        <%= f.check_box :remember_me ,class:"form-check-input p-2",type:"checkbox", style:"margin-right:10px" %>
                        <label class="form-check-label" for=""> Ricorda la password </label>
                    </div>
                <% end %>

                <div class="actions">
                    <%= f.submit "Login",class:"btn btn-danger btn-lg btn-block" %>
                </div>
                <% end %>

            <div class="pt-3">
                <%= link_to "Non ho ancora un account", new_registration_path(resource_name), class:"link-secondary" %><br />
            </div>
            <hr class="my-4">

            <%- if devise_mapping.omniauthable? %>
                <%- resource_class.omniauth_providers.each do |provider| %>
                    <%= form_for "Login",
                    url: omniauth_authorize_path(resource_name, provider), 
                    method: :post,
                    data: {turbo: "false"} do |f| %>
                        <%= f.submit "Accedi con Google", type: "submit", class:"btn btn-lg btn-block btn-dark" %>
                    <% end %>
                <% end %>
            <% end %>

            
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
</body>
</html>